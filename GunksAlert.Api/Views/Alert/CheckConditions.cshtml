@model GunksAlert.Api.Models.ConditionsReport

@{
    ViewData["Title"] = "Alerts";
}

<div class="model-container d-flex flex-column">
    <div class="w-100">
        <h2>Climbability Report</h2>
    </div>
    <div class="report-body">
        @if (Model != null) {
            <ul>
                <li><strong>Date:</strong> @Model.Date.ToString("MMM d, yyyy")</li>
                <li><strong>Crag:</strong> The Gunks</li>
                <li><strong>Climbable:</strong> @(Model.IsClimbable() ? "Yes" : "No")</li>
                <li><strong>Temperature:</strong> @Model.TempFeelsLike</li>
                <li><strong>Chance dry:</strong> @(Model.ChanceDry * 100)%</li>
                <li><strong>Wind:</strong> @Model.WindSpeed</li>
                <li><strong>Clouds:</strong> @Model.Clouds</li>
                <li><strong>Humidity:</strong> @Model.Humidity</li>
            </ul>
        } else {
            <p>No report found</p>
        }
    </div>
</div>
